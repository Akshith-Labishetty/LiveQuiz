rabbitmq ->Pushes into queue
username : guest
pswd : guest
localhost:15672/#/

rabbitmq-service start
cd BACKEND/celery
celery -A celery_app worker -l info
cd BACKEND
node server.js

Student submits quiz
        ↓
Node.js sends task to RabbitMQ
        ↓
RabbitMQ stores the task
        ↓
Celery Worker picks the task
        ↓
Worker saves result in MongoDB
        ↓
Node.js/Redis sends updates to teacher dashboard

             Logging                      Monitoring
Node.js ──────────▶ Logstash        Node.js ──────────▶ Prometheus

Celery  ───────────▶ Logstash       Celery  ───────────▶ Prometheus

RabbitMQ(optional) ─▶ Logstash      RabbitMQ(optional) ─▶ Prometheus

